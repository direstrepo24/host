import React from 'react';

// Exporta componentes y utilidades compartidas
export const sharedConfig = {
  version: '1.0.0',
};

// Example shared component
export const ExampleComponent: React.FC = () => {
  return <div>Shared Component Example</div>;
};
